<Incrementador>:
    orientation: 'vertical'
    Button:
        text:'Incrementar'
        font_size: 30 
        on_release: label1.text = str(int(label1.text)+1)
    Label: 
        id: label1
        text: '1'
        font_size: 30 